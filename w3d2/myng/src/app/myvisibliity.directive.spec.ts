import { MyvisibliityDirective } from './myvisibliity.directive';

describe('MyvisibliityDirective', () => {
  it('should create an instance', () => {
    const directive = new MyvisibliityDirective();
    expect(directive).toBeTruthy();
  });
});
